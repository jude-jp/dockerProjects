ARG VERSION=apache

ARG TARGETPLATFORM 

FROM php:${VERSION}

RUN "Building for $TARGETPLATFORM platform"

RUN sed -i '1s/^/ServerName localhost\n/' /etc/apache2/apache2.conf

COPY . /var/www/html/

EXPOSE 80